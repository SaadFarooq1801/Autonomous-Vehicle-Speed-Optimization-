# Autonomous-Vehicle-Speed-Optimization-

This code simply takes data given to it, in this case traffic signs, trains a AI model using that data, and tests that AI model using another dataset with similar data. Then outputs a graph with the model accuracy and a h5 file of the model.
